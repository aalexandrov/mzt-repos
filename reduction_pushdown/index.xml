<?xml-stylesheet type="text/xsl" href="index.xsl"?>
<queries>
  <query>
    <id>e362fbdd</id>
    <sql>SELECT DISTINCT
    R.x,
    R.y,
    R.z,
    R.y / R.z
FROM
    R
INNER JOIN 
    (VALUES (1),(2)) AS S(x) ON R.x = S.x</sql>
  </query>
  <query>
    <id>d5be225f</id>
    <sql>SELECT DISTINCT
    R.x,
    R.y,
    R.z,
    R.y / R.z
FROM
    R
INNER JOIN 
    (VALUES (1)) AS S(x) ON R.x = S.x</sql>
  </query>
</queries>
